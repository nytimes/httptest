tests:
  # - description: 'Homepage'
  #   request:
  #     path: '/'
  #   response:
  #     status: 200
  #     headers:
  #       match:
  #         - abc: 'def'
  #       pattern:
  #         - abc: 'a.*'
  #       exclude:
  #         - abc: 'a.*'

  # - description: 'favicon'
  #   request:
  #     path: '/favicon.ico'
  #   response:
  #     status: 403
  #     headers:
  #       match:
  #         - abc: 'def'
  #       pattern:
  #         - abc: 'a.*'
  #       exclude:
  #         - abc: 'a.*'

  # - description: 'abc123'
  #   request:
  #     path: '/aa'
  #   response:
  #     status: 404

  - description: 'hp'
    request:
      path: '/'
    response:

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200
