tests:
  # - description: 'Homepage'
  #   request:
  #     path: '/'
  #   response:
  #     status: 200
  #     headers:
  #       pattern:
  #         - abc: 'a.*'
  #       exclude:
  #         - abc: 'a.*'

  # - description: 'favicon'
  #   request:
  #     path: '/favicon.ico'
  #   response:
  #     status: 403
  #     headers:
  #       match:
  #         - abc: 'def'
  #       pattern:
  #         - abc: 'a.*'
  #       exclude:
  #         - abc: 'a.*'

  # - description: 'abc123'
  #   request:
  #     path: '/aa'
  #   response:
  #     status: 404

  - description: 'baidu hp'
    envFilter:
      - TEST_ENV:
        - dev
        - stg
    request:
      address: 'www.baidu.com'
      path: '/'
    response:
      status: 200
      headers:
        patterns:
          server: '^BWS'
        exclude:
          - 'abcdefg'
      body:
        patterns:
          - '14px.*?"宋体"'

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200

  - description: 'hp'
    request:
      path: '/'
    response:
      status: 200
